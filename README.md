# 5 Noches en PbtServers (FNAF) Web

## Local Hosting
```sh
# clone the repo
git clone https://github.com/PbtServers/PbtFNAF-Web.git

# install the dependencies
npm install

# start the server
npm start
```
